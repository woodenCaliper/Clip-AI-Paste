# 要求仕様書：Clip AI Paste

## 1. 概要

本ツールは、Chrome拡張として動作する。

ユーザーがショートカットキーを1回押すと、クリップボードの最新テキストを取得し、設定済みプロンプトと組み合わせてAIへ送信する。

AIから返ってきた回答は、自動的にクリップボードへ格納され、通常の貼り付け操作と同等の挙動で現在の貼り付け先へ貼り付けられる。

ユーザーに追加で `Ctrl+V` などの貼り付け操作は要求しない。

---

## 2. 利用形態

- 実行環境：Google Chrome
- 実装形態：Chrome拡張
- 利用方法：ローカルで作成し、自分のChrome環境に入れて使用する
- 配布：しない
- 利用者：自分のみ

---

## 3. 基本動作

ショートカットキー押下時に、以下の処理を1コマンドで実行する。

1. ショートカットキーの押下を検知する
2. クリップボードの最新テキストを取得する
3. 取得したクリップボード内容を、設定済みプロンプトと組み合わせる
4. 組み合わせた内容を、選択中のAIへ送信する
5. AIの回答を取得する
6. AI回答をクリップボードへ格納する
7. 自動で貼り付けを実行する

---

## 4. ショートカットキー要件

起動操作はショートカットキーとする。

当初想定キーは `Ctrl+B` とする。

ショートカットキーはカスタムできる仕様にする。

登録するショートカットキーは1つのみとする。

複数ショートカットの登録は不要とする。

---

## 5. クリップボード要件

入力元はクリップボードとする。

対象は、クリップボードの最新内容とする。

対象データはテキストのみとする。

画像、ファイル、HTML、リッチテキストは対象外とする。

クリップボードが空、またはテキストでない場合は何もしない。

AI回答を貼り付けるために、AI回答を一度クリップボードへ格納する。

元のクリップボード内容は復元しない。

AI回答を格納した後、クリップボードはAI回答で上書きされたままでよい。

---

## 6. 貼り付け要件

AI回答は、`Ctrl+V` が押されたときと同じように貼り付ける。

ただし、ユーザーに `Ctrl+V` を押させない。

ショートカットキー1回で、クリップボード読み込みからAI回答の貼り付けまで完了する。

入力欄に既存文字がある場合も、独自に挿入位置や置換方法を制御しない。

通常の `Ctrl+V` と同じ挙動とする。

入力欄がない場合も、通常の `Ctrl+V` と同じ挙動とする。

---

## 7. プロンプト要件

プロンプトは1つのみとする。

プロンプト切り替え機能は不要とする。

プロンプトは設定画面で設定できるようにする。

AIへ送る本文は、AIがプロンプトとクリップボード本文を正しく区別できる形式にする。

送信形式例：

```text
# 指示

{プロンプト}

# 入力テキスト

{クリップボード本文}
```

---

## 8. AI連携要件

AIへ内容を送信し、回答を取得する。

対応AIは以下の3つとする。

- OpenAI
- Gemini
- Claude

使用するAIは、上記3つのうち1つを選択する。

Custom API URLや任意APIエンドポイントには対応しない。

使用するAIは設定画面で選択できるようにする。

---

## 9. APIキー要件

以下のAPIキーを設定できるようにする。

- OpenAI APIキー
- Gemini APIキー
- Claude APIキー

APIキーはChrome拡張の設定画面で設定する。

使用中のAIに対応するAPIキーを使用する。

---

## 10. モデル名要件

モデル名は自由入力とする。

候補リストや固定選択式は不要とする。

モデル名はChrome拡張の設定画面で設定する。

AIごとのモデル名管理については、設定画面で扱えるようにする。

---

## 11. AIパラメータ要件

AI送信時の追加パラメータ設定は不要とする。

以下の設定項目は不要とする。

- temperature
- 最大トークン数
- その他生成パラメータ

---

## 12. 確認画面要件

AIへ送信する前の確認画面は不要とする。

AI回答を貼り付ける前の確認画面も不要とする。

ショートカットキー押下後は、確認なしで処理を実行する。

---

## 13. ログ要件

ログ機能は不要とする。

以下は保存しない。

- AIへの送信内容
- AIの回答
- 実行履歴
- エラー履歴

---

## 14. 設定画面要件

APIキー、使用AI、モデル名、プロンプトは、Chrome拡張の設定画面で設定する。

設定画面には以下の項目を用意する。

- 使用AI
- OpenAI APIキー
- Gemini APIキー
- Claude APIキー
- モデル名
- プロンプト
- ショートカット設定へのリンク

設定バックアップ機能は不要とする。

設定のエクスポート・インポート機能も不要とする。

---

## 15. エラー通知要件

エラー発生時は作業者に通知する。

ただし、エラーメッセージをユーザーが閉じる操作は不要にする。

エラー通知方式は実装側に任せる。

作業を妨げない通知方式とする。

以下の場合はエラー通知する。

- AI APIから空の回答が返った場合
- AI APIリクエストが失敗した場合
- APIキー未設定など、設定不足がある場合

以下の場合は通知しない。

- クリップボードが空の場合
- クリップボード内容がテキストでない場合

---

## 16. 処理中表示要件

処理中表示は必要とする。

ただし、うっとうしくない表示にする。

表示方式は実装側に任せる。

作業の邪魔にならない控えめな表示とする。

---

## 17. タイムアウト要件

AI APIのレスポンス待ち時間は10秒までとする。

10秒を超えた場合はタイムアウトとして扱う。

タイムアウト時はエラー通知する。

---

## 18. 連続実行要件

同じショートカットキーが連続で押された場合は、最後の実行を優先する。

先に開始していた処理は破棄する。

複数処理の同時完了は許容しない。

---

## 19. ページ制限要件

貼り付け対象ページの制限は設けない。

許可リスト、禁止リストは不要とする。

実行可能なページでは実行する。

---

## 20. 拡張名・アイコン

拡張名は `Clip AI Paste` とする。

アイコンは実装側で決める。

---

## 21. 要求一覧

| ID | 要求 |
|---|---|
| R-001 | Chrome拡張として作成する |
| R-002 | Chrome Web Store からインストールして利用できる |
| R-003 | 一般利用者への公開配布（Chrome Web Store）を前提とする |
| R-004 | ショートカットキーで処理を起動できる |
| R-005 | ショートカットキーはカスタムできる |
| R-006 | 登録するショートカットキーは1つだけとする |
| R-007 | 当初想定キーは `Ctrl+B` とする |
| R-008 | クリップボードの最新内容を取得できる |
| R-009 | クリップボードの対象はテキストのみとする |
| R-010 | クリップボードが空、またはテキストでない場合は何もしない |
| R-011 | クリップボード内容と特定のプロンプトを組み合わせられる |
| R-012 | プロンプトは1つだけとする |
| R-013 | プロンプト切り替え機能は不要とする |
| R-014 | プロンプトは設定画面で設定できる |
| R-015 | AIへ送る本文は、プロンプトと入力テキストをAIが区別できる形式にする |
| R-016 | 組み合わせた内容をAIへ送信できる |
| R-017 | 対応AIはOpenAI、Gemini、Claudeの3つとする |
| R-018 | Custom API URLには対応しない |
| R-019 | 使用するAIはOpenAI、Gemini、Claudeのうち1つを選択する |
| R-020 | OpenAI APIキーを設定できる |
| R-021 | Gemini APIキーを設定できる |
| R-022 | Claude APIキーを設定できる |
| R-023 | APIキーは設定画面で設定できる |
| R-024 | モデル名は自由入力できる |
| R-025 | モデル名は設定画面で設定できる |
| R-026 | AI送信時の追加パラメータ設定は不要とする |
| R-027 | AIの回答を取得できる |
| R-028 | AI APIから空の回答が返った場合はエラー通知する |
| R-029 | AI APIリクエストが失敗した場合はエラー通知する |
| R-030 | AI APIのタイムアウトは10秒とする |
| R-031 | AI回答は一度クリップボードへ格納する |
| R-032 | AI回答をクリップボードへ格納した後、自動で貼り付けを実行する |
| R-033 | ユーザーに追加で `Ctrl+V` などの貼り付け操作をさせない |
| R-034 | 処理はショートカットキー1回で完結する |
| R-035 | AI回答を `Ctrl+V` と同じ挙動で貼り付ける |
| R-036 | 入力欄に既存文字がある場合も、通常の `Ctrl+V` と同じ挙動にする |
| R-037 | 入力欄がない場合も、通常の `Ctrl+V` と同じ挙動にする |
| R-038 | 元のクリップボード内容は復元しない |
| R-039 | 送信前の確認画面は不要とする |
| R-040 | 貼り付け前の確認画面は不要とする |
| R-041 | ログ機能は不要とする |
| R-042 | APIキー、使用AI、モデル名、プロンプトはChrome拡張の設定画面で設定する |
| R-043 | 設定画面にはショートカット設定へのリンクを用意する |
| R-044 | 設定バックアップ機能は不要とする |
| R-045 | 設定のエクスポート・インポート機能は不要とする |
| R-046 | 設定不足時はエラー通知する |
| R-047 | エラー時は作業者へ通知する |
| R-048 | エラー通知は、ユーザーが閉じる操作を必要としない方式にする |
| R-049 | 処理中表示を行う |
| R-050 | 処理中表示はうっとうしくない方式にする |
| R-051 | 連続実行時は最後の実行を優先する |
| R-052 | 連続実行時、先に開始していた処理は破棄する |
| R-053 | 貼り付け対象ページの制限は設けない |
| R-054 | 拡張名は `Clip AI Paste` とする |
| R-055 | アイコンは実装側で決める |
| R-056 | 利用者向けに、動作対象外ページ（例: `chrome://`、Chrome Web Store）を明記する |
| R-057 | 利用者向けに、権限（clipboard / activeTab / storage）の利用目的を明記する |
| R-058 | 利用者向けに、問い合わせ先（Issue 等）を明記する |

# User-provided custom instructions

言語は日本語で会話してください

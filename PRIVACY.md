# Privacy Policy / プライバシーポリシー

最終更新日: 2026-05-03

本拡張機能（Clip-AI-Paste）は、ユーザーが明示的に実行した機能の提供に必要な範囲で、以下の情報を取り扱います。

## 1. 収集・処理するデータ

本拡張機能が収集・処理する主なデータは次のとおりです。

- **クリップボード内容**  
  ユーザーが貼り付け・整形・変換等を実行する際に、対象テキストを処理します。
- **APIキー**  
  OpenAI / Gemini / Claude の各API呼び出しに使用するため、ユーザーが設定したAPIキーを取り扱います。
- **プロンプト設定**  
  ユーザーが設定したシステムプロンプト・変換ルール等を保存し、API呼び出し時に使用します。

## 2. データ送信先

ユーザーがAI実行を行った場合、必要なデータ（入力テキスト、プロンプト設定、選択モデルに応じた認証情報）が、以下の外部APIに送信されます。

- OpenAI API
- Google Gemini API
- Anthropic Claude API

※実際の送信先は、ユーザーが選択したプロバイダに依存します。

## 3. 保存場所

拡張機能の設定データ（APIキー、プロンプト設定、選択中のプロバイダ設定など）は、**`chrome.storage.sync`** に保存されます。

## 4. 保存期間・削除方法

- 保存期間: ユーザーが削除するまで保持されます。
- 削除方法:
  1. 拡張機能のオプション画面から設定項目をクリア
  2. Chrome の拡張機能管理画面から本拡張機能を削除
  3. Chrome の同期データ削除操作（Googleアカウント側の同期データ管理）

上記操作により、`chrome.storage.sync` に保存された当該データの削除が可能です。

## 5. 問い合わせ窓口

- 連絡先: GitHub Issues
- URL: `https://github.com/<YOUR_ACCOUNT>/<YOUR_REPOSITORY>/issues`

※公開時には `<YOUR_ACCOUNT>` と `<YOUR_REPOSITORY>` を実際の値に置き換えてください。
